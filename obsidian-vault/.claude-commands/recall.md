Найди в `obsidian-vault/` всё, что связано с темой: $ARGUMENTS

Действия:
1. Поищи через `grep -ri` по содержимому файлов в obsidian-vault/
2. Проверь имена файлов на совпадения
3. Проверь wiki-links `[[...]]` на упоминания темы

Выдай:
- Список найденных файлов с путями
- Краткая выжимка из каждого релевантного файла
- Связи между заметками (если есть)

Не добавляй новую информацию — только то, что уже есть в vault.
