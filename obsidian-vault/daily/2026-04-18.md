---
date: 2026-04-18
type: daily
---

# 2026-04-18 — Сессия

## Что сделано
- **Перенос лендинга в корень репо** (`landing/` → корень):
  - `landing/index.html`, `legal.html`, `reviews.html`, `assets/` перемещены в корень
  - Обновлены ссылки: `../login.html` → `login.html` в index/legal/reviews
  - Обновлены ссылки в `login.html`: `landing/index.html` → `index.html`, `landing/legal.html` → `legal.html`
  - Папка `landing/` удалена
  - `.gitignore` обновлён (медиа-пути без префикса `landing/`)
- **Настроен FTP доступ к Hostinger**:
  - `.env` с FTP кредами (host `145.79.25.241`, user `u848244871`, port 21)
  - Правильный путь обнаружен: `domains/sairateam.com/public_html`
  - `.env.example` как шаблон
  - Подключение проверено через `curl` в Git Bash — работает
- **Диагностика расхождений** между тремя версиями лендинга:
  - Локальный `index.html` (594 стр) — вырезаны hero-video, секция галереи (pool/suite/restaurant/port), readonly поле реф-кода
  - Хостинговый `index.html` (643 стр) — все фичи на месте, но нет кнопки Кабинет, старый футер, битые ссылки `../login.html`
  - Desktop `C:\Users\Syrym\Desktop\SYSTEM v2.1\landing\` — ещё одна промежуточная версия
- **Выяснено про палитру**: локальный и хостинговый CSS оба в палитре Ocean Breeze (светло-голубая, `--ocean #0077B6`, `--breeze #CAF0F8`, glass white). "Грязно-зелёной" Premium Marine (`#0B3D2E`) уже нигде нет.

## Решения
- **Вариант Б** (перенос landing в корень) — принят, выполнен локально
- **FTP вместо Git deploy** — выбран как способ работы с хостингом
- **План восстановления фич**: взять хостинговую версию (`index.html`, `legal.html`, `style.css`) как базу, накатить сверху фиксы (Кабинет, новый футер, `login.html`). Палитру не трогать — уже светлая
- **Favicon**: SVG с буквой "s" в Ocean Breeze палитре (пользователь исходник не дал)
- **`public_html/landing/` на хостинге пустая** — "старый лендинг" по URL `/landing/` это только кэш браузера, удаление папки + очистка кэша решит

## На следующую сессию
- Подтвердить план восстановления и выполнить:
  1. Скопировать хостинговые `index.html`, `legal.html`, `style.css` в локальный репо
  2. Накатить фиксы: кнопка Кабинет, новый футер с ИП Султанова, `../login.html` → `login.html`
  3. `reviews.html` — добавить Кабинет + новый футер
  4. Добавить favicon SVG во все 4 HTML
  5. Залить всё через FTP, удалить `public_html/landing/`
  6. Проверить в инкогнито `https://sairateam.com/` — должны работать легал/отзывы/кабинет
- Если конкретные элементы покажутся тёмными — точечно осветлить

## Заметки
- **Service worker от старого сайта** может продолжать показывать старый лендинг даже после уборки. Если проблема останется — DevTools → Application → Service Workers → Unregister
- **Пароль FTP** содержит `#` — в bash-присваивании это нормально, но при передаче в URL нужно URL-escape (`%23`) — иметь в виду при сборке команд
- **Dev-команда в CLAUDE.md** устарела: теперь открывается по `http://127.0.0.1:3000/?ref=TEST001` (без `/landing/`)
- **`tmp_hosting/`** в корне — временная папка с дампом хостинговых файлов для сравнения, можно почистить или добавить в .gitignore

---

# 2026-04-18 — Вечерняя сессия

## Что сделано
- **План восстановления лендинга выполнен полностью:**
  - FTP download хостинговых `index.html`, `legal.html`, `login.html`, `reviews.html` в `tmp_hosting/`
  - `index.html`: взял хостинговый (643 стр) как базу, накатил фиксы — кнопка «Кабинет» в навбаре, футер с ИП Султанова, `../login.html` → `login.html`. Hero-video + галерея (pool/suite/restaurant/port) восстановлены
  - `legal.html`, `reviews.html` — локальные уже правильные, только favicon
  - `login.html` — фикс пути `dashboard/css/` → `dashboard/assets/css/`
  - Favicon SVG (Ocean Breeze, буква "s") создан в `assets/images/favicon.svg`, подключён во все 4 HTML
  - FTP upload всех 4 HTML + favicon.svg; пустая `public_html/landing/` удалена
- **Восстановление пароля на login.html:**
  - Ссылка «Забыли пароль?» под формой входа
  - Форма `#form-forgot` → `POST /auth/v1/recover?redirect_to=...`
  - Форма `#form-reset` → `PUT /auth/v1/user` с новым паролем (token из URL hash)
  - `switchTab` расширен на `forgot`/`reset`, авто-открытие reset при `#access_token=...&type=recovery`
- **Выход на лендинг из кабинета (все 9 вкладок):**
  - Логотип `system v2` → ссылка на `../index.html`
  - Кнопка `🏠 На лендинг` в правой части шапки (с responsive: иконка на мобилке)
  - Правка в одном месте — `dashboard.js` + CSS, применяется сразу ко всем вкладкам
- **Dashboard вкладка «Заявки» (`leads.html`) заполнена:**
  - 4 stat-карточки (Новые/В работе/Квалифицированы/Конверсия)
  - Фильтр-пилюли по статусу + поиск по имени/телефону/email/городу
  - 8 mock-лидов, статусы: new/contacted/qualified/converted/rejected
  - Клик → модалка с полными данными + смена статуса + кнопки AI/переписка
  - Без кнопки «Новая заявка» (правило из CLAUDE.md)
  - Фикс `padding-right:44px` у `.modal-head` — бейдж больше не наезжает на крестик
- **Dashboard вкладка «Контакты» (`contacts.html`) заполнена:**
  - 4 stat-карточки (Всего/WhatsApp/Telegram/Приглашены)
  - Фильтры по тегам (Семья/Друзья/Работа/Клиенты/Знакомые/Приглашены) + поиск
  - 8 mock-контактов разных тегов и мессенджеров (whatsapp/telegram/instagram/viber)
  - Клик → модалка с действиями:
    - 📨 Пригласить — копирует ref-ссылку `sairateam.com/?ref=TEST001` в clipboard
    - 🤖 AI-приглашение — toast-заглушка (реализация позже)
    - Связаться — открывает нативный мессенджер (`wa.me/`, `t.me/`, `viber://`, и т.д.)
    - ✏️ Редактировать — toast; 🗑 Удалить — работает локально
  - «➕ Добавить» и «📥 Импорт» — toast-заглушки до миграции БД
- **Память / vault:**
  - `feedback_leads_readonly.md` — правило «лиды только через форму, партнёр не создаёт»
  - `AI Agent.md` → секция «UX-решения для кнопок в модалке лида» (полуавтомат + `lead.html?id=X`)

## Решения
- **Лендинг восстановлен** — хостинговый index.html как база + фиксы (Кабинет, футер ИП, правильные ссылки, favicon). Вариант А подтверждён и выполнен
- **Проверка login через Supabase Dashboard → Add user** (Вариант A вместо регистрации через форму) — флоу прошёл успешно
- **Выход на лендинг: 2 способа** — кликабельный логотип + явная кнопка «🏠 На лендинг»
- **Заполнение вкладок по одной**, чтобы Сырым контролировал расход токенов
- **Для «Контакты»: Вариант Б** — UI на mock-данных; миграция БД отложена. Схема `public.contacts` в Supabase (`id, lead_id, channel, value, created_at`) не соответствует «телефонной книге партнёра» из CLAUDE.md — это каналы лидов. План миграции: переименовать → `lead_channels`, создать новую `contacts` с полями (partner_id, name, phone, email, messenger, tag, notes, source, imported_from)
- **AI-агент работает в полуавтомате:** черновик от LLM → партнёр редактирует/одобряет → отправка через WABA/Telegram. Причина: WABA банит за нарушения Meta policy, LLM может случайно пообещать доход (InCruises policy)
- **Переписка с лидом — отдельная страница** `lead.html?id=X`, а не split-view в модалке. Причина: длинная переписка, deep-link из уведомлений, back-button

## На следующую сессию
- Продолжить заполнение вкладок дашборда (осталось 6):
  - tasks, structure, history, training, templates, settings
- После заполнения всех вкладок:
  - Миграция БД для `contacts` (переименовать текущую → `lead_channels`, создать новую)
  - Создать страницу `lead.html?id=X` — timeline `lead_messages` + input для отправки
  - Форма добавления контакта вручную + импорт CSV/VCF/Google
  - Реализация реального AI-агента: n8n workflow + LLM API + интеграция с `ai_jobs`, `lead_messages`, `events_log`
- Архитектура: `public.partners` не имеет `user_id` → связь с `auth.users` отсутствует. Нужен триггер или ручной sync при регистрации через login.html

## Заметки
- **В `auth.users` пусто**, тестовый партнёр существует только в `public.partners` (нет пароля). Для теста логина пришлось создать юзера через Supabase Dashboard → Add user (Auto Confirm)
- **Для работы «Забыли пароль» в проде** нужно проверить Supabase URL Configuration: Site URL = `https://sairateam.com`, Redirect URLs должен содержать `https://sairateam.com/login.html`
- **Toast-уведомления** добавлены глобально (`.toast` в dashboard.css) — переиспользуются в `leads.html` и `contacts.html`
- **Модалка закрывается** по: backdrop click / крестик / Escape
- **Ref-код партнёра** в `contacts.html` пока захардкожен `const MY_REF = 'TEST001'` — после реализации привязки auth.users ↔ partners брать из sessionStorage

---

# 2026-04-18 — Ночная сессия (вкладки дашборда 4–8)

## Что сделано
- **Заполнены 5 вкладок дашборда** в едином UX-паттерне (stat-карточки → ряды фильтров → поиск → список → модалка действий) и залиты на хост через FTP (все HTTP 226):
  - **`tasks.html`** (22 869 байт) — to-do партнёра. 11 mock-задач, источники AI/Я/Система, типы (звонок/сообщение/follow-up/встреча/напоминание), приоритеты, фильтры по сроку (Просрочено/Сегодня/Завтра/На неделе/Выполнено) и источнику. Snooze (+1ч/+3ч/На завтра/+1д/+1н), сортировка просрочка→ближайший→done в конец. Зафиксирован `TODAY = 2026-04-18` для предсказуемых mock-сроков
  - **`structure.html`** (27 102 байт) — MLM-дерево. Прогресс ранга SMD→RD ($1500/$2000), реф-блок с copy, 16 mock-партнёров на 3 уровнях с indent, фильтры по линии (L1/L2/L3+) и статусу (active/inactive/new), цветовая кодировка 8 рангов (ED/ID/ND/RD/SMD/MD/Active/New). Sidebar: Топ-5 по объёму с медалями + сводка по линиям. Модалка с действиями (копировать реф-ссылку партнёра, перейти в его команду через downline)
  - **`history.html`** (25 197 байт) — read-only журнал событий из `events_log`. 22 mock-события за 2 недели, группировка по дням («Сегодня · 18 апр», «Вчера», «понедельник»). 3 ряда фильтров: период (Всё/Сегодня/Вчера/7д/30д) + категория (Лиды/Сообщения/Контакты/AI/Партнёры/Задачи/Логин) + актёр (Я/AI/Система/Партнёр). Модалка с meta-данными (lead_id, ip, device, status_from/to). Явная сноска «Удаление и редактирование запрещены»
  - **`training.html`** (28 238 байт) — обучение. Большой блок «Общий прогресс» с %, бар, сводка. 10 модулей в card grid (auto-fill 290px) с прогресс-барами и цветовым акцентом (синий→золотой→зелёный). Бейджи: уровень (Базовый/Средний/Продвинут), ⚠ Обязательно, 🏆 Сертификат. Модалка модуля со списком уроков (Видео/Статья/Чек-лист/Тест/Практика) — клик по уроку отмечает выполнение, прогресс обновляется в реальном времени
  - **`templates.html`** (30 022 байт) — шаблоны сообщений. 13 mock-шаблонов на реальных кейсах InCruises (warm_intro_v2, objection_pyramid, qualified_present_invite, sys_consent_confirm и т.д.). Бейджи каналов в фирменных цветах (WA зелёный / TG синий / IG розовый / Email голубой). 3 ряда фильтров (категория/канал/статус). **Двухрежимная модалка**: «📝 Шаблон с переменными ↔ 👁 Preview с подстановкой» — переменные `{{name}}`, `{{city}}` подсвечены, в preview подставляются примеры. Дублирование, активация-деактивация, копирование в clipboard

## Решения
- **Единый UX-паттерн для всех 8 заполненных вкладок**: stat-карточки сверху (4 шт.) → ряды pill-фильтров (`.pt`) → поиск (`.sch-i`) → список (`.tl/.ti` или card grid) → действия в модалке (`.modal-grid + .modal-actions`)
- **Tasks: фиксированный TODAY-курсор** (не `new Date()`) для mock-данных — иначе сроки "просрочено/сегодня/завтра" разъезжаются при просмотре в разные дни
- **History — строго read-only**: явная сноска про `events_log`, нет кнопок редактирования/удаления, только просмотр + meta. Это аудиторский след
- **Training: обязательные модули — gate для AI** — новое UX-правило: «Без обязательных модулей AI-агент не активируется». Это защита от compliance-нарушений (партнёр сначала обязан пройти Compliance-модуль и Старт)
- **Templates: двухрежимная модалка** — partner может посмотреть исходный шаблон с переменными И preview с подставленными данными. Это критично перед одобрением AI-черновиков (полуавтомат-флоу из AI Agent.md)
- **«Белые блоки» — кэш браузера**: получили промт про якобы белые карточки на дашборде. Проверили: файлы на хосте 1-в-1 с локальными, в CSS никаких white-фонов нет. Это был кэш / service worker от старого сайта. Ничего не правили — пользователь подтвердил, что после очистки кэша всё нормально

## На следующую сессию
- **Заполнить последнюю 9-ю вкладку — `settings.html`** (роли, AI-настройки, интеграции)
- После этого вернуться к большому списку:
  - Миграция БД для `contacts` (текущая → `lead_channels`, новая «телефонная книга партнёра» с partner_id, name, phone, email, messenger, tag, notes, source, imported_from)
  - Создать страницу `lead.html?id=X` — timeline `lead_messages` + input для отправки
  - Связь `auth.users ↔ public.partners` (нужен триггер или sync при регистрации через login.html)
  - Форма добавления контакта вручную + импорт CSV/VCF/Google
  - Реальный AI-агент через n8n + LLM API + интеграция с `ai_jobs`, `lead_messages`, `events_log`
  - Для прода «Забыли пароль» — проверить Supabase URL Configuration (Site URL + Redirect URLs)

## Заметки
- **Кэш / service worker — постоянная проблема**. Когда заливаешь обновления HTML через FTP, сразу делать hard-refresh (Ctrl+Shift+R) или открывать в инкогнито. Если service worker от старого сайта живёт — F12 → Application → Service Workers → Unregister
- **FTP-заливка**: размеры файлов локальный vs хост — отличный sanity-check перед заливкой. Если идентичны — заливать не нужно. Сегодня сэкономили на leads/contacts/css/js (уже совпадали)
- **Расхождение по палитре в промте «белых блоков»**: пользователь описал Premium Marine (`#0B3D2E`/`#1C1F26`/`#C97D4E`), но дашборд сейчас в Ocean Breeze (`#0A1929`/`#0077B6`/`#00B4D8`). Премиум Марин был выпилен ещё утром. Если когда-то решит вернуть — будет отдельный палеточный refactor
- **Дата-курсоры в mock-данных** (`TODAY = 'YYYY-MM-DD'`) лучше делать константами в каждой mock-вкладке, а не `new Date()` — при просмотре в другой день mock-сроки разъезжаются
- **Templates body** — обычные строки с `{{var}}`-плейсхолдерами, рендер через regex. В реальной БД это будет JSONB с массивом vars + body-шаблон, чтобы валидировать что все переменные определены
