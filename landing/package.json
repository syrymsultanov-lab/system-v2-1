{
  "name": "system-v2-landing",
  "version": "1.0.0",
  "description": "System v2 landing — локальный сервер для разработки",
  "scripts": {
    "dev": "http-server . -p 3000 -c-1 --cors -o"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
