# Workflow architecture report

**Workflow:** WF2 — Lead Dedup & Normalize
**Date:** 29.03.2026, 17:15:38
**Health score:** 80/100

## Summary
Local Analysis: 6 issues found.

## Category scores
| Category | Score | Status |
| --- | --- | --- |
| Security | 100 | Good |
| Performance | 85 | Good |
| Reliability | 40 | Needs work |

## Findings (6)

### 🔴 CRITICAL

#### Missing global error workflow
**Issue:** No error workflow configured

**Fix:** Assign a global error workflow in settings

Without a global error workflow, failures in non-critical nodes may go unnoticed. Every production workflow should define a handler for uncaught exceptions.

---

### 🟡 WARNING

#### Data bloat risk
**Issue:** Execution data saving is active

**Fix:** Disable 'Save data' for production

Saving all executions (success/error) fills the n8n database quickly, causing slowness and crashes.

---

#### Fragile external call
**Issue:** No retry or continue on fail

**Fix:** Enable RetryOnFail

**Affected nodes:** Fetch Recent Leads, Flag Lead Event, Log Dedup

External APIs are unstable. Enable 'Retry on Fail' for transient errors.

---

#### Unhandled IF branch
**Issue:** IF node 'false' branch unconnected

**Fix:** Handle both branches explicitly

**Affected nodes:** Any Duplicates?

Unconnected IF branches silently drop items, which may cause data loss.

---

### 🔵 Info

#### Timeout not configured
**Issue:** No execution timeout set

**Fix:** Set a workflow timeout

Workflows without timeouts can become 'zombie processes' that consume resources indefinitely if they hang.

---

#### Default timezone
**Issue:** Timezone is not explicit

**Fix:** Define specific timezone

Relying on system timezone can cause errors in Cron jobs if the server is migrated.

---

_Generated by Audit8n - n8n Workflow Analyzer_
